# A structure repository documenting my journey learning python with a focus on building strong foundation for backend development.
# The repository contains python exercise,implementations,experiments and small projects covering both fundamentals and intermediate concepts.

## D1

# - @ is reassignment: add = log_call(add)
# - Closure = inner function + captured enclosing scope. Decorators are closures
# - No wraps → __name__ is wrapper → FastAPI's /docs would show every route as wrapper
# - Decorator with arguments = 3 layers, because retry(3) is called before it decorates