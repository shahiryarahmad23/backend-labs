name: sql_labs
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: sql_labs
      POSTGRES_PASSWORD: sqlabs_password
      POSTGRES_DB: sql_lab_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
