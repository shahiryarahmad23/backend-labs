# A structured repository for learning and practicing SQL with focus on developing the database knowledge required for backend development.
# This repository will contain SQL queries,exercise,database concepts and practical examples as i progress from fundamentals to more advance topics.
 
## diff loop Raw_sql and ORM query:

- The Raw sql the i wrote in the raw_join.sql and ORM gave the same result but in sql query i had to write the alias for the duplicate column name but in the ORM query the ORM handled it on its own i only wrote the table names 

- The orm separates the params for where clause to protect the SQL injection

